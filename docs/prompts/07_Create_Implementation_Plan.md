Create the Implementation Plan for:

Milestone 7: HTTP API Endpoints - RED

Inspect the current repository first.

Create:
docs/.ai/007_Implementation_Plan_HTTP_API_RED.md

Include proposed HTTP tests for exactly:
- POST /api/v1/reservations
- GET /api/v1/reservations/{id}
- DELETE /api/v1/reservations/{id}

Cover 201, 200, 204, 400, 404, 409, JSON mapping, UUID parsing,
Content-Type, and concurrent POST behavior.

Include concrete test code.

Do not modify source/test files.